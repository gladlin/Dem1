<Page x:Class="Dem1.Requests"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Dem1"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Заявки партнеров">

    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="190*"/>
            <ColumnDefinition Width="350*"/>
            <ColumnDefinition Width="120*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="1" HorizontalAlignment="Right">
            <Button x:Name="btnChangePartner" Content="Редактировать партнёра" Visibility="Hidden" Click="btnChangePartner_Click" Width="150"  Background="#FF0C4882" Foreground="White" Grid.Row="0" Margin="5"/>
            <Button x:Name="btnShowRequests" Content="Показать заявки пртнёра" Visibility="Hidden" Click="btnShowRequests_Click" Width="150"  Background="#FF0C4882" Foreground="White" Grid.Row="0" Margin="5"/>
        </StackPanel>
        <Button x:Name="btnAddRequest" Content="Добавить заявку"  Click="btnAddRequest_Click" Grid.Row="0" Grid.ColumnSpan="3" Margin="5" Background="#FF0C4882" Foreground="White"  HorizontalAlignment="Right"/>

        <ListView Name="LViewProduct" Grid.Column="0" Grid.ColumnSpan="4" Grid.Row="1" Margin="5, 5, 5, 5" d:ItemsSource="{d:SampleData ItemCount=1}" SelectionChanged="LViewProduct_SelectionChanged" MouseDoubleClick="LViewProduct_MouseDoubleClick">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black" BorderThickness="1" Background="{Binding Background}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Margin="5">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding PartnerType}" FontSize="16"/>
                                    <TextBlock Text="{Binding PartnerName, StringFormat=' | \{0}'}" FontSize="16"/>
                                </StackPanel>

                                <TextBlock Text="{Binding URIAddress}"/>
                                <TextBlock Text="{Binding PhoneNumber}"/>
                                <TextBlock Text="{Binding Ratio, StringFormat='Рейтинг: \{0}'}"/>
                            </StackPanel>

                            <TextBlock Grid.Column="2" Text="{Binding Cost, StringFormat='\{0:F2} руб.'}" VerticalAlignment="Center" FontSize="16" HorizontalAlignment="Center"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
